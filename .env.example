
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db:5432/spring_liberation_rose"

# ---- Authentication ----
NEXTAUTH_URL="http://localhost:3000"
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET=""

# ---- Docker Compose ----
POSTGRES_USER="postgres"
POSTGRES_PASSWORD=""
POSTGRES_DB="spring_liberation_rose"

# ---- Mail (SMTP) ----
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
# Optional: defaults to SMTP_USER if not set
SMTP_FROM=""
